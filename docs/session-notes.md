# Notes de session — reprise sur la VM Windows

Ce fichier résume ce qu'une session Claude Code sans historique (ex: sur la VM Windows) doit savoir pour reprendre le développement sans repartir de zéro. Le cahier des charges (`docs/cahier-des-charges.md`) reste la référence pour la vision produit et les specs fonctionnelles complètes — ce document-ci couvre le processus de travail et l'état d'avancement.

## Où on en est

- Toute l'UI (Dashboard prêt/bloqué, Paramètres, Actualités/Changelog, Premier lancement, modales Réparation/Mise à jour) est implémentée — voir `docs/cahier-des-charges.md` §6.1 pour le détail écran par écran.
- **Sous-projet #1 (Fondations Steam & VDF) est terminé** — `ISteamEnvironment` est réel sur Windows (`SteamEnvironment` : détection registre `SteamPath`, `SteamLibraryLocator`/`SteamWorkshopReader` pour le parsing VDF/ACF multi-bibliothèques via `Gameloop.Vdf`). Spec : `docs/superpowers/specs/2026-07-29-steam-vdf-foundations-design.md`.
- **Sous-projet #2 (Gestion du mod Java / agent) est terminé** — `IJavaModService` est réel sur Windows (`JavaModService` : détection présence/SHA-256 des fichiers du mod Java via `JavaFileInspector`, vérification de l'option de lancement Steam `-agentlib:zbNative --` via `SteamLaunchOptionInspector`, manifeste distant via `JavaModManifestFetcher`, réparation avec téléchargement + vérification d'intégrité avant remplacement atomique des fichiers). Spec : `docs/superpowers/specs/2026-07-29-java-mod-agent-foundations-design.md`, plan : `docs/superpowers/plans/2026-07-29-java-mod-agent-foundations.md` (plan local, non committé par convention — voir historique de commits `42cf94e`..`26c8185` sur `main` pour le détail). Fusionné en local sur `main`, **pas encore poussé vers `origin`** au moment de cette note.
  - **Limitation connue, attendue** : l'URL du manifeste JSON du mod Java est un placeholder — aucun hébergement réel n'existe encore. Concrètement, `GetStatusAsync()` retournera toujours une liste de fichiers vide sur une vraie machine tant que l'hébergement n'est pas en place ; le check Dashboard "Mod Java à jour" affichera "Impossible de vérifier le mod Java." à moins que l'option de lancement Steam soit elle-même absente (auquel cas ce message-là prime, l'ordre des checks a été volontairement inversé par rapport au plan d'origine pour garder l'état de l'option de lancement observable — voir review finale de ce sous-projet).
  - **Vérification manuelle encore à faire sur la VM Windows** (pas faisable en session sans historique) : confirmer que le check d'option de lancement Steam bascule réellement quand on configure `-agentlib:zbNative --` dans les propriétés du jeu ; confirmer que `RepairAsync` peut écrire dans `steamapps/common/ProjectZomboid` sans élévation sur une installation Steam par défaut.
- **Limitation connue, attendue** : `IServerInfoService` est toujours entièrement Fake (`FakeServerInfoService`) — buildid requis, IDs Workshop requis (`server.json`) sont codés en dur et ne correspondront à aucune installation réelle. Concrètement, sur une vraie machine Windows, `CanPlay` restera bloqué (Version/Workshop en échec, et maintenant aussi Mod Java tant que son manifeste n'est pas hébergé) tant qu'un vrai service d'info serveur n'existe pas. Le check Workshop affiche volontairement un message qualitatif ("collection non à jour") plutôt qu'un nombre de mods manquants, car ce nombre serait basé sur des données factices.
- **Prochain chantier convenu** : remplacer les Fakes restants par de vrais services Windows, dans cet ordre (chacun avec son propre cycle spec → plan → implémentation) :
  1. ~~Fondations Steam & VDF~~ — fait.
  2. ~~Gestion du mod Java / agent~~ — fait.
  3. **Packaging Velopack & CI** — nature différente (infra de build/release, pas un service runtime). Probablement en dernier. **À reprendre ensuite** — pas encore brainstormé, aucune spec écrite.
  4. *(Non planifié formellement mais nécessaire à terme)* **Vrai `IServerInfoService`** — fetch de `server.json`/`mods.json` (buildid requis, IDs Workshop requis, changelog, news) depuis l'hébergement statique (§8.3 du cahier des charges), **et** un hébergement réel pour le manifeste du mod Java (`docs/cahier-des-charges.md` §8.3 également). Sans ça, `CanPlay` ne passera jamais sur une vraie machine même avec #1 et #2 terminés.

## Workflow établi cette session (à respecter)

1. **Brainstorming** (skill `superpowers:brainstorming`) — dialogue de clarification, jamais de code avant validation du design par l'utilisateur.
2. Spec écrite dans `docs/superpowers/specs/YYYY-MM-DD-<sujet>-design.md`, **committée** dans git.
3. **Plan** (skill `superpowers:writing-plans`) — plan détaillé tâche par tâche, écrit dans `docs/superpowers/plans/YYYY-MM-DD-<sujet>.md`, **jamais committé** (reste local uniquement — convention établie dès le premier plan de cette session).
4. **Implémentation** (skill `superpowers:subagent-driven-development`) — un subagent implémenteur frais par tâche, puis double review systématique (conformité au spec, puis qualité de code), boucle de correction si des issues Important/Critical sont trouvées. Les issues Minor n'imposent pas de boucle systématique — jugement au cas par cas.
5. Toujours dans un **git worktree** dédié, jamais directement sur `main` — skill `superpowers:using-git-worktrees`. Sur la VM Windows (avec l'outil natif `EnterWorktree` disponible), les worktrees sont créés sous `.claude/worktrees/<nom>/` (pas `.worktrees/`, qui est le repli manuel du skill en l'absence d'outil natif) et gérés/nettoyés via `ExitWorktree`, pas `git worktree remove` à la main.
6. Une fois toutes les tâches faites : review finale de l'ensemble du diff, puis skill `superpowers:finishing-a-development-branch` (fusion locale, PR, ou autre selon le choix de l'utilisateur).
7. Vérification visuelle UI : toujours par comparaison capture d'écran fournie par l'utilisateur ↔ maquette originale, jamais d'automatisation de clics fiable trouvée sur macOS — laisser l'utilisateur tester interactivement.

## Conventions de code établies

- **DI** : ViewModels de page (`DashboardViewModel`, `SettingsViewModel`, etc.) = `AddSingleton` (identité stable requise pour la navigation, `ViewLocator` ne les re-résout jamais). ViewModels de **modale** = construits manuellement à la demande, jamais enregistrés comme singleton (état jetable, recréé à chaque usage).
- **Erreurs/succès** : pattern `StatusMessage` (string?) + `IsStatusSuccess` (bool), avec le converter partagé `BoolToStatusBrushConverter` pour la couleur.
- **Converters** : instance statique `Instance`, brushes en hex littéral (pas de binding vers les resources XAML live, pour éviter le couplage), `ConvertBack` lève `NotSupportedException`.
- **`src/GlasLauncher.App` n'a PAS `ImplicitUsings` activé** (contrairement à `GlasLauncher.Core.Tests`, qui l'a) — usings explicites obligatoires dans tout nouveau fichier `.cs` de ce projet.
- **Boutons alignés en paire** : toujours `StackPanel Orientation="Horizontal"` + `MinWidth` sur chaque bouton — **jamais** `Grid ColumnDefinitions="*,*"` avec des boutons censés "stretch" (bug constaté : ils ne remplissent pas la colonne comme attendu dans ce projet).
- **Wrapping de texte à côté d'une icône/puce** : un `StackPanel Orientation="Horizontal"` donne une largeur illimitée à ses enfants dans l'axe d'empilement, donc `TextWrapping="Wrap"` ne s'applique jamais dedans (le texte déborde silencieusement). Utiliser un `Grid` (colonne `Auto` pour l'icône/puce, colonne `*` pour le texte) à la place — voir `DashboardView.axaml` (liste des vérifications) pour le pattern déjà en place.
- **`App.axaml`** : `RequestedThemeVariant="Dark"` forcé — l'app a une identité visuelle sombre fixe, ne doit jamais suivre le thème clair/sombre de l'OS (bug constaté : sur un Windows en thème clair, le texte des boutons sans `Foreground` explicite devenait quasi invisible).
- **Fenêtre custom (`SystemDecorations="None"`)** : la barre de titre a un `PointerPressed` câblé vers `BeginMoveDrag` pour permettre de déplacer la fenêtre — nécessaire dès qu'on utilise un chrome de fenêtre personnalisé. `SystemDecorations="None"` supprime aussi le redimensionnement natif par les bords même avec `CanResize="True"` — recréé manuellement dans `MainWindow.axaml`/`.axaml.cs` via une grille de zones de 6px sur les bords/coins appelant `BeginResizeDrag(WindowEdge, e)`.
- **Icônes de boutons (barre de titre, etc.)** : préférer une forme vectorielle (`Rectangle`/`Path`/`Ellipse`, `Stroke`/`Fill="{Binding $parent[Button].Foreground}"` pour hériter la couleur) plutôt qu'un glyphe Unicode en `Content` — bug constaté : le rendu d'un glyphe (position verticale, "carré" visuellement écrasé) dépend de la police et ne s'aligne pas de façon fiable avec les boutons voisins.
- **Contenu de hauteur variable dans un `DockPanel`** (ex. sidebar avec liste de vérifications qui peuvent afficher un message d'erreur en plus) : le dernier enfant d'un `DockPanel` (qui remplit l'espace restant) ne se limite pas tout seul — s'il dépasse la place disponible, il chevauche visuellement les enfants ancrés (ex. pied de page `Dock="Bottom"`) au lieu d'être coupé. Envelopper ce contenu dans un `ScrollViewer` (`VerticalScrollBarVisibility="Auto"`, invisible tant que ça tient).

## Environnement

- Repo cloné sur la VM ; `git pull` pour récupérer jusqu'au commit `391c96b` (et au-delà si du travail a été poussé depuis).
- Outils installés via `setup.ps1` (dossier partagé UTM, **hors du repo git**, ne pas chercher ce script dans le repo) : .NET 8 SDK, Git, GitHub CLI, Node.js, Claude Code.
- Build : `dotnet build` (solution complète) ou `dotnet build src/GlasLauncher.App` / `dotnet build src/GlasLauncher.Core`.
- Tests : `dotnet test tests/GlasLauncher.Core.Tests` (seul projet testé — pas de tests unitaires sur la couche App/ViewModels par convention, vérifié par les reviews spec/qualité à la place).
- Lancer l'app : `dotnet run --project src/GlasLauncher.App`.
